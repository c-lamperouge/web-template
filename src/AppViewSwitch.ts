const enum ViewName {
  Fallback = 'Fallback',
  Test = 'Test',
  Login = 'Login',
}

const enum ViewTransitionType {
  None,
  SlideLeft,
  SlideRight,
}

export {
  ViewName,
  ViewTransitionType
}
